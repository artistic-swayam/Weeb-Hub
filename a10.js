function countVowels(str){
    let count = 0;
    for(const char of str){
        if(
        char === "a" ||
        char === "a" ||
        char === "a" ||
        char === "a" ||
        char === "a" ||)
        {
        count++;
        }
    }
    return(count);
}